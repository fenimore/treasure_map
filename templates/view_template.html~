<!DOCTYPE html>
<html>
  <head>
    <title>Recent Stuff</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="static/s.css" rel="stylesheet" media="screen">
    <link href="static/normalize.css" rel="stylesheet" media="screen">
  </head>
  <body>
    <div class="container">
      <h1>Recent items in {{ location.title() }}</h1>      
      <button class="button-primary">Click <a href="/{{ location }}/map">here</a> to see the map.</button>
      <br>
      <div class="row">
        <div class="one column">Select:</div>
        <div class="four columns">      
          {% for thing in things[:5] %}
          <div border="solid">
            <h4>{{ thing.place.title() }}</h4>
            <a href ={{ thing.url }} target="_blank">
            <span title={{ thing.title }}>
            <img 
                src= {{ thing.image }} width="200" 
                alt= {{ thing.title }}  height=auto />
            </span>
            </a>
          </div>
          
          {% endfor %}
        </div>
        <div class="four columns">
          {% for thing in things[5:] %}
          <div border="solid">
            <h4>{{ thing.place.title() }}</h4>
            <img href ={{ thing.url }} 
                src= {{ thing.image }} width="200" height=auto
                 alt= {{ thing.title }}  />
          </div>
          {% endfor %}
        </div>
      </div>

      <br>
      <hr>

    </div>
  </body>
</html>
